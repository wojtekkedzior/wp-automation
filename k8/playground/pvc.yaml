apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mypvc
  name: mypvc
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 60G
  storageClassName: fast-disks
  volumeMode: Filesystem
